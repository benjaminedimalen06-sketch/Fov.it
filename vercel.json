{
  "version": 2,
  "builds": [
    { "src": "api/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/s/(.*)", "dest": "/view.html" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
